#include <stdio.h>
#include <math.h>

int main()
{
    double A = sqrt(17);
    double B = pow(2, 4);
    int C = round(4.99);
    int D = ceil(3.14);
    int E = floor(3.99);
    double F = fabs(-20);//lf turn to positive number
    double G = log(3);
    double H = sin(45);

    printf("%lf", H);

    return 0;

}