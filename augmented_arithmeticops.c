#include <stdio.h>

/*
 * augmentedoperators- replace statements where an operator takes a variable
 * and assigns results back to the same operator
 */
int main()
{

int x = 10;

//x += 2;
//x -= 1;
//x /= 2;
printf("%d",x);

return 0;
}